# ADR 0003: Import Legacy Maps Once

- Status: Accepted
- Decision: legacy `.MAP` files are converted into canonical rewrite content and are not the long-term runtime source.
- Consequence: `:game-legacy` is import-only infrastructure.

