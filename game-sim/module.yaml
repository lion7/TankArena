product:
  type: lib
  platforms: [jvm]

dependencies:
  - ../game-core
  - ../game-content
  - ../game-input
  - ../game-protocol
  - $compose.desktop.currentOs
  - io.github.pandulapeter.kubriko:engine-desktop:0.0.8
  - io.github.pandulapeter.kubriko:plugin-collision-desktop:0.0.8

settings:
  kotlin:
    version: 2.3.20
    serialization:
      format: json
      version: 1.11.0
  compose:
    enabled: true
    version: 1.10.0
